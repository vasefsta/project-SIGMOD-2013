name: sigmod-2013

on:
  push:
    branches: [ part1 ]
  pull_request:
    branches: [ part1 ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make run
      run: make run CFLAGS=-fPIC
